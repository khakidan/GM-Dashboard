# Roadmap

Referenced from the root [AGENTS.md](../../AGENTS.md). This file tracks **only currently-open work** — pending features/bugs and in-progress/scoped-but-not-yet-built plans. Read this file (not `CHANGELOG.md`) to know what's currently being worked on or planned next.

Per root AGENTS.md rule 12: when something here is completed, it gets **removed entirely** from this file (not archived here) and a write-up documenting what was actually built gets added to [CHANGELOG.md](CHANGELOG.md) instead. This file should stay small and fully current — if a section here says "Completed," that's a sign it should have already been moved out.

---

## Pending Features

Features and bugs that have been discussed and approved but not yet implemented. Each entry contains enough context to implement without further discussion.

### 🔴 Bugs to Fix

None currently open.

### 🟡 Features to Add

**Full Codebase Audit (bugs → componentization → UI uniformity).** Comprehensive pass for logic errors/bugs, oversized files needing decomposition, and UI/UX uniformity closer to established D&D apps (D&D Beyond, Roll20 conventions). Prioritized in this order: (1) bug-hunting/logic errors, (2) file size & componentization, (3) UI uniformity/D&D-app UX conventions. **Phase 1 (bug-hunting) is fully complete — see `CHANGELOG.md`.** **Phase 2 (componentization) is actively in progress, with substantial work still open** — detailed below. **Phase 3 (UI uniformity) has not yet started.**

**Phase 2 status**: the 6 cinematic overlay components' consolidation (video-layer hook, fade keyframes, film grain layer, `formatNames()`) is fully complete — see `CHANGELOG.md`. Everything below this point is still open, unimplemented work — a systematic 6-category audit (className strings → JSX structure → utility functions → hook patterns → data structures/constants → server-side patterns) surfaced 30 real, evidence-backed findings, of which **none have been implemented yet** — only investigated, verified, and logged. This is a large remaining backlog, not a wrapped-up phase.

**Leads from this session's own file-size census, not yet investigated as deeply as the systematic audit below**: `NpcFormFields.tsx`'s `Abilities`/`Stat Block` tabs are still inline while `Identity`/`Combat` were already extracted — an inconsistent decomposition worth finishing. `useParty.ts` (592 lines after its prior pure-function extraction) and `useCombatSync.ts`/`ActiveEncounterTab/index.tsx` are worth a fresh size check given everything layered on since. `campaigns.ts`'s inlined 7-sheet schema definition overlaps conceptually with `schema.md` and `useSheetSync.ts`'s expectations — three places that need to agree on one schema, currently with no single source of truth. A reusable loading-spinner component was also noticed (inline `Loader2` used with slightly varying sizes across several buttons/dialogs) but not yet scoped. **Server-side error response shaping** — `campaigns.ts` and `auth.ts` both hand-build `{ error, message }` JSON error responses with a similar shape but not identical conventions; not a UI component, but the same "one place should own this format" logic applies, worth a real comparison before deciding if it's worth consolidating. **Possibly a shared tabbed-entity-form pattern** (`NpcFormFields.tsx` and `NewPlayerDialog.tsx` are structurally similar — Identity/Combat/Abilities + one more tab) — genuinely unverified whether this is real duplication or the same kind of surface-level similarity that the `CardHeader` investigation correctly found didn't hold up once compared directly; needs an honest side-by-side check, not an assumption either way, before treating it as a real candidate.

**Systematic componentization audit — Category 1 (repeated className strings), findings logged, not yet independently verified beyond items 5/6 below, not yet implemented.**

1. **"Primary Card / Panel Container"** — `bg-white p-6 rounded-2xl border border-[#e2e8f0] shadow-sm space-y-4`, reported in 5 files: `AuthPortalSettings.tsx`, `ReferenceDataSeeder.tsx`, `GMTestingTools.tsx`, `SettingsPage.tsx`, `SheetConnectionSettings.tsx`. Not yet independently verified.
2. **"Card / Panel Header Title"** — `text-lg font-bold text-[#0f172a] font-serif pb-1`, reported in 3 files (`ReferenceDataSeeder.tsx`, `GMTestingTools.tsx`, `SheetConnectionSettings.tsx`), paired with item 1's containers. Not yet independently verified.
3. **"Card / Panel Header Subtitle"** — `text-xs text-[#8d8db9]`, reported in the same 3 files as item 2, directly beneath the item 2 titles. Not yet independently verified.
4. **"Form / Section Label"** — `block text-xs font-bold uppercase tracking-widest text-[#8d8db9] mb-1.5 px-1`, reported 13 times across `IdentityTab.tsx` (5), `CombatTab.tsx` (5), `NewEncounterDialog.tsx` (3). Note: `NpcFormFields.tsx` defines an equivalent `labelClass` that resolves to the same string when not compact — deliberately excluded from this count as a dynamic/already-abstracted case, not raw duplication. Not yet independently verified.
5. **"Compact / Sub-Field Label"** — `block text-[10px] font-semibold text-[#8d8db9] uppercase px-1`, real duplication confirmed to be narrower than the original file count implied: **`NpcCard.tsx` and `NpcFormFields.tsx` each independently supply this exact label markup into `NpcCombatActionFields.tsx`'s `secondaryField`/`range` slots** (for the Recharge, Range, and Legendary Cost fields specifically) — those slots were deliberately built as generic `React.ReactNode` rather than typed config, by design, not left duplicated by oversight; the two calling files ended up re-implementing the identical JSX as a direct consequence. This is genuinely 2 files duplicating slot content, not 3 files independently duplicating a standalone pattern.
6. **"Compact Text Input / Textarea Field"** — `w-full bg-white border border-[#e2e8f0] rounded-xl outline-none transition-all font-serif italic text-sm py-1 px-2 focus:border-[#2563eb] focus:ring-1 focus:ring-[#2563eb]` — same root cause and same 2 real files (`NpcCard.tsx`, `NpcFormFields.tsx`) as item 5, for the same Recharge/Range/Cost slot-content fields. `NpcSimpleFieldEditor.tsx` also uses this exact class internally for its own name input — that one's a legitimate single internal use, not duplication.

**Items 5 and 6 together point to one real, well-understood fix**: either extract a small shared "serif italic compact input" sub-component that `NpcCard.tsx`/`NpcFormFields.tsx` can both pass into the `secondaryField`/`range` slots instead of hand-rolling the JSX each time, or give `NpcCombatActionFields.tsx` a few additional typed, optional props (e.g. `rechargeValue`/`onRechargeChange`, `rangeValue`/`onRangeChange`, `costValue`/`onCostChange`) so it can render these standard cases itself and the slot-based `ReactNode` escape hatch is only needed for genuinely non-standard cases. Not yet decided which approach is better, and not yet implemented either way.

**Systematic componentization audit — Category 2 (repeated JSX structural patterns), findings logged, not yet implemented.**

7. **"Labeled-Field Stacked Form Layout"** — a label-over-input shape reported 14 times across `IdentityTab.tsx` (5), `CombatTab.tsx` (5), `NewEncounterDialog.tsx` (3), `ShortRestDialog.tsx` (1). **Verified directly against the existing `LabeledField.tsx` before accepting this as a "new" finding — it isn't simply unadopted duplication of an already-solved problem.** `LabeledField.tsx` currently: renders a plain `<div>` for its label (no `<label htmlFor>` pairing, breaking the accessibility link these 4 real call sites already correctly have), types `label` as a bare `string` (can't accept the required-field asterisk or the dynamic trailing note `ShortRestDialog.tsx` needs), and hardcodes `text-[10px] mb-2` (smaller than these 4 sites' actual `text-xs mb-1.5`). **Real remediation is upgrading `LabeledField.tsx` itself** (accept `React.ReactNode` for `label`, add an optional `htmlFor`, add a size variant) before these 4 sites could adopt it — not just wider adoption of what already exists.
8. **Loading-spinner-plus-text button pattern** — a real, genuine gap in `Button.tsx`'s existing `loading` prop: it replaces `children` entirely with a bare spinner, so any button needing descriptive loading text alongside the icon (not just a spinner) has to bypass `Button` and hand-roll a raw `<button>`. Confirmed in 4 files: `CampaignSelector.tsx` (2 instances — "Creating spreadsheet...", "Connecting..."), `PartyTab.tsx` ("Adding..."), `ReferenceDataSeeder.tsx` ("Seeding..."). `EncountersTab.tsx`'s equivalent button was checked directly per request and confirmed structurally similar but without descriptive text (icon swap only), so it's a slightly different case.
9. **Two-column form field grid** (`grid grid-cols-2 gap-4`) — reported 5 times across 3 files. Likely too generic/low-abstraction-value to warrant a shared component (same category as the "empty-state displays" pattern from the original Shared UI Component Consolidation audit, which was correctly judged not worth componentizing) — not yet decided either way, flagged for a real decision rather than defaulting to "build it."
10. **Comma-separated pill/value parsing** — `.split(',').map(s => s.trim()).filter(Boolean)` genuinely duplicated 3 times (`CombatantIrvDisplay.tsx`, `ConditionChips.tsx`, `IrvMultiSelect.tsx`). Worth being precise about what's actually shared here: only `CombatantIrvDisplay.tsx` renders this as read-only pills; the other two use the identical parsing logic for their own internal interactive multi-select state, not for the same rendering pattern. Real candidate is a **shared parsing utility function** (same category as item 16 below), not a shared rendering component across all 3.

**Rediscovered loose threads from earlier sessions — identified/discussed at the time but never followed up on, still not implemented.** Found via a deliberate review of the full prior-session transcript archive, not just this session's own history:

11. **Section headers shared component — fully scoped and decision made, ready to build whenever implementation begins.** Confirmed pattern: `text-[#8d8db9] text-xs font-bold uppercase tracking-widest border-b border-[#e2e8f0] pb-1 mb-2`, rendered as `h3`, across `ShortRestDialog.tsx`, `LevelUpDialog.tsx`, `LevelUpResourcePools.tsx`, `LevelUpChecklist.tsx` (verbatim match, all 4). Two real, confirmed points of drift: `ReferenceDetailDialog.tsx` uses `text-[10px]` instead of `text-xs` (a deliberate smaller size for a nested section — maps to a `size?: 'compact' | 'default'` prop) and `NpcStatBlockSection.tsx` uses `font-semibold`/`tracking-wider`/`h4` instead of `font-bold`/`tracking-widest`/`h3`, and already takes a dynamic `{title}` prop. Visually compared both treatments side by side before deciding (not just judged from class names) — confirmed the difference doesn't read as meaningful; **decision made: normalize `NpcStatBlockSection.tsx` to match the other 5 exactly, no variant needed.** Component design: `children` (must support `LevelUpChecklist.tsx`'s trailing aside-span pattern, not just plain text), `size?: 'compact' | 'default'`. Confirmed via codebase-wide search that no other instances of this pattern exist beyond these 6, and that `ShortcutCheatSheet.tsx`/`LabeledField.tsx`/`NpcListEditor.tsx` are genuinely different patterns, correctly out of scope. Not yet built.
12. **`AudioLibrary.tsx`'s remaining `hover:text-red-550` typo** on 2 buttons ("Remove assignment," "Clear all") — explicitly deferred during the `IconButton` migration as out of scope for that task, "noted for a future fix" that never happened. Small, contained, no design question — just needs the same typo fix already applied to every other instance of this bug elsewhere in the same file.
13. **Short-rest combatant-mirroring test coverage gap** (`useParty.ts`/`usePartyRest.test.ts`) — deliberately deferred during the long-rest combatant-mirroring fix ("Short-rest combatant-mirroring coverage remains a separate, not-yet-added gap"), never picked back up. Test coverage, not a code fix.
14. **`combatantBuilder.test.ts`'s stable-PC-rebuild scenario** — flagged as an open item after the turn-skip bug fix ("worth checking if this area gets touched again"), never actually verified one way or the other. Test coverage, not a code fix.

**Systematic componentization audit — Category 3 (repeated utility/logic functions), findings logged, not yet implemented (except item 15, already done via the overlay work).**

15. ~~`formatNames()`~~ — **already extracted, see `CHANGELOG.md`.** Same finding as the overlay investigation, confirmed via this broader audit to have exactly 2 occurrences codebase-wide, no additional undiscovered copies exist elsewhere.
16. **Comma-separated list parsing (`.split(',').map(...).filter(Boolean)` and close variants)** — genuinely widespread: confirmed in 14 distinct files across 3 real variants — (a) the exact verbatim chain in 9 files (`useDeathSaves.ts`, `useCombatantExpanded.ts`, `useBatchActions.ts`, `useCombatConcentration.ts`, `useHealthChange.ts` (×2 lines, 1 file), `CombatantIrvDisplay.tsx`, `IrvMultiSelect.tsx`, `ConditionChips.tsx`, `conditionDefinitions.ts`); (b) a lowercase-mapping variant in 3 files (`CombatantCardBadges.tsx`; `CombatantCardExpanded.tsx`; `combatLogic.ts` line 129 inside `getEffectiveResistances`); (c) a no-`filter(Boolean)` variant, also missing the trim-before-lowercase ordering, in 3 files (`conditions/index.ts`, `concentrationCheck.ts`, `combatLogic.ts` line 265 inside `getExpiredConditions`). Overlaps with item 10 above (same underlying parsing logic). Strong, well-evidenced candidate for a single shared `parseCommaSeparatedList()` utility.
17. **`isConcentrating()`** — two independently-implemented versions with slightly different null-handling: `conditions/index.ts`'s version defaults `conditions ?? ''` before processing; `concentrationCheck.ts`'s version early-returns `false` on a falsy/whitespace-only input via `!conditions?.trim()`. Both end up case-insensitive and functionally equivalent for valid input — a real duplicate worth consolidating to one implementation.
18. **Bonus/modifier sign formatting (`formatBonus`-style functions)** — 3 near-identical implementations: `StatBlockScores.tsx`'s `formatBonus` (returns bare `'0'` for a zero value), `SpellcastingStatsRow.tsx`'s `formatSpellAttackBonus` and `CombatantCardHeader.tsx`'s inline logic (both return `'+0'` for zero) — the only real behavioral difference across all 3 is that one zero-value edge case. Worth deciding whether `'0'` or `'+0'` is actually correct for a modifier display before consolidating, then extracting to one shared function.
19. **Concentration check DC calculation** — `concentrationCheck.ts`'s exported `concentrationCheckDc()` (already the consolidated single source of truth per the earlier `lib/` bug-hunt audit) is not actually being called everywhere it should be: `useHealthChange.ts` hand-rolls the identical formula (`Math.max(10, Math.floor(damage / 2))`) inline inside a toast description string instead of calling the existing helper. Not a "duplicate implementation to merge" so much as "an existing shared function not being used at one real call site" — a smaller, more targeted fix than the other items here.
20. **Ad hoc random-ID generation** — 7 files each hand-roll their own `Math.random()`/`Date.now()`-based ID string with no shared helper: `useCampaign.ts` (a full RFC4122-shaped UUID fallback), `combatLog.ts`'s `generateEventId()`, `useEncounterPresetLoader.ts` (4 separate inline uses of the same pattern), `NpcListEditor.tsx` (`generateKey()`, added during the `NpcListEditor.tsx` key-stability fix), `AudioLibrary.tsx`, `audioFileStore.ts`, `googleAuth.ts`. Genuinely varied in exact format/purpose (some need UUID-shape, some just need a unique-enough string) — worth checking whether a single shared "generate a unique id" utility with a couple of format options would actually simplify these, or whether the variation is legitimate and this is lower priority than the others here.
21. **Server-side Google API error-response shaping** — `auth.ts` and `campaigns.ts` both independently implement the identical logical pattern (read error body as text, attempt `JSON.parse` with a catch-fallback, log, respond) but with genuinely different fallback shapes (`auth.ts`: `{ error: 'GOOGLE_API_ERROR', message: errorText }`; `campaigns.ts`: `{ error: { message: errorText } }`, then reshaped again into its own final response structure). Same underlying "handle a possibly-non-JSON upstream error" logic, worth a shared helper.

**Systematic componentization audit — Category 4 (repeated hook patterns), findings logged, not yet implemented.**

22. **Local-state-plus-commit-on-blur/Enter reimplemented as one-offs** — beyond the already-shared `DebouncedInput.tsx`/`DebouncedTextarea.tsx`, this exact pattern is separately reimplemented in `InitiativeInput.tsx`, `CardNumberInput.tsx` (itself already a shared component, so this is somewhat expected/acceptable), and `NpcIdentityTab.tsx` (which defines a completely standalone local `LocalTextInput` inline in the file rather than reusing anything shared). Worth checking whether `NpcIdentityTab.tsx`'s one-off specifically could just adopt `DebouncedInput`/`CardNumberInput` directly instead of maintaining its own copy.
23. **"Sync local state from a prop via `useEffect`"** — extremely widespread: 10 distinct files, 11 instances (`DebouncedInput.tsx`, `DebouncedTextarea.tsx`, `ConditionChips.tsx`, `IrvMultiSelect.tsx`, `CardNumberInput.tsx`, `NpcIdentityTab.tsx`, `StatBlockScores.tsx` ×2, `InitiativeInput.tsx`, `CharacterCard.tsx`, `EncounterCard.tsx`). This is a very common, low-risk React pattern in general (controlled-component prop-sync), so breadth alone doesn't necessarily mean it's a good extraction candidate — most of these are already inside components that have good reason to hold local editable state distinct from their prop. Flagged for a real decision on whether any single custom hook (e.g. a generic `useSyncedLocalState(prop)`) would actually simplify these meaningfully, or whether this is just idiomatic React repeated correctly many times, not a real duplication problem.
24. **Debounce-via-`setTimeout` (distinct from the timer-race bugs already fixed)** — this shape (a timer ref, cleared and rescheduled on each call) appears in `CharacterCard.tsx` (temp AC, 600ms), `IrvMultiSelect.tsx` (300ms), `ConditionChips.tsx` (300ms). Worth checking whether any of these 3 additional instances have the same latent race condition (rapid successive calls racing against each other's stale timers) already fixed elsewhere this session — not yet checked, flagged for verification before assuming these are safe just because they haven't been reported as broken.
25. **"Auto-dismissing temporary UI state"** (a `setState` paired with a `setTimeout` that blindly clears it after a fixed delay) — found in 6 files: `AuthRelay.tsx`, `EncounterLogDetails.tsx`, `EncounterCard.tsx`, `useCombatSync.ts`, `CombatantCard.tsx`, `DiceRoller.tsx`. A plausible candidate for a small shared `useAutoDismiss(value, ms)`-style hook, though genuinely worth checking whether any of these 6 have the same "no cancellation of a previous pending timer" race condition already fixed elsewhere this session before treating this as purely a componentization nicety rather than a possible bug — not yet checked.

**Systematic componentization audit — Category 5 (repeated data structures/constants), findings logged, not yet implemented.**

26. **Spreadsheet row schemas defined twice** — `campaigns.ts`'s inline sheet-header arrays (used when seeding a new campaign) and `sheetSchemas.ts`'s Zod tuple schemas (used when parsing synced data back) independently encode the identical column order for all 4 core sheets (Characters: 26 columns, NPCs: 22, Encounters: 7, Encounter_Combatants: 14) — confirmed with real quoted field lists matching exactly, not just similar. No single shared source of truth; the two could silently drift if either is changed without updating the other.
27. **D&D class keys hardcoded across 4 separate maps in 3 files**, with an inconsistent casing convention worth flagging on its own: `hitDice.ts`'s `CLASS_HIT_DIE_MAP` uses lowercase keys (`barbarian`, `fighter`, ...), while `spellcasting.ts`'s `SPELLCASTING_ABILITY_MAP`/`CLASS_SAVING_THROW_MAP` and `classResources.ts`'s `CLASS_RESOURCE_SUGGESTIONS` all use Titlecase (`Barbarian`, `Fighter`, ...). Not confirmed whether this casing mismatch is already safely handled everywhere via case-insensitive lookups or could be a latent bug for any future case-sensitive consumer — worth checking before assuming it's fine.
28. **Standard conditions list duplicated exactly** — the same 20 condition strings defined independently in `irvOptions.ts`'s exported `CONDITION_OPTIONS` and re-declared inline inside `ConditionPopover.tsx`'s component body as `standardConditions`. Worth considering whether importing from `irvOptions.ts` here instead of hand-maintaining a separate copy would prevent this exact category of drift bug (the earlier `'aid'`/`'aid (boosted)'` fix in this same file) from recurring.
29. **Spell effects list duplicated exactly** — same pattern and same 2 files as item 28: `irvOptions.ts`'s `EFFECT_OPTIONS` vs. `ConditionPopover.tsx`'s inline `spellEffects` array, 20 matching items confirmed.
30. **`DamageType` union type hand-duplicated from its own runtime array** — `irvOptions.ts`'s `DAMAGE_TYPE_OPTIONS` (a runtime string array) and `types.ts`'s `DamageType` union type independently list the same 16 damage type strings. A standard, safe TypeScript pattern exists to derive the union directly from the array (`type DamageType = typeof DAMAGE_TYPE_OPTIONS[number]`) instead of maintaining both by hand.
31. **Ability score abbreviation array hardcoded in 2 places instead of importing the existing export** — `StatBlockScores.tsx` already exports `abilitiesInOrder: AbilityName[] = ['STR', 'DEX', 'CON', 'INT', 'WIS', 'CHA']`, but `spellcasting.ts` and `abilityScores.ts` each separately hardcode the identical inline array for their own validation checks instead of importing the existing constant.

**Systematic componentization audit — Category 6 (repeated server-side patterns), findings logged, not yet implemented. This is the last of the 6 systematic categories — the audit itself is complete, but implementation of its findings has not started.**

32. **`req.body` existence check duplicated verbatim** across both route files (`campaigns.ts`, `auth.ts`) — identical condition and response shape. Real cross-file duplication, good candidate for shared middleware.
33. **Rate-limiter base configuration duplicated across both route files** — `campaignCreateLimiter` (`campaigns.ts`) and `authLimiter` (`auth.ts`) share the identical `windowMs`/`max`/`standardHeaders`/`legacyHeaders` values. Worth noting precisely: only the *shape* of the config matches — the actual `message` field differs between the two, so any shared factory would need to accept a per-route message while sharing the rest.
34. **Google Client ID env-var fallback chain duplicated within a single file** — `auth.ts` reads the identical `process.env.VITE_GOOGLE_CLIENT_ID || process.env.CLIENT_ID || process.env.GOOGLE_CLIENT_ID` chain independently in both its `GET /config` and `POST /google-token` handlers. Smaller fix — a single module-level constant at the top of `auth.ts`.
35. **Google Sheets API fetch headers duplicated within a single file** — `campaigns.ts` constructs the identical `{ 'Content-Type': 'application/json', 'Authorization': 'Bearer ${token}' }` headers object inline for 3 separate fetch calls. Within-file duplication, worth a small local helper.

### 🔵 Architecture / Technical Debt

**Remaining Technical Debt:** None.

**Future audit categories, discussed but not yet started** — beyond bug-hunting and componentization (Phase 1/2 above), a professional React/TypeScript codebase review typically also covers several categories this project hasn't yet run as a dedicated pass:

- **Type safety** — a systematic sweep for `any`/unsafe type casts, missing null checks, and places where TypeScript's strictness is being worked around rather than honored. Only ever caught incidentally so far (e.g. the `as any` shortcut proposed and rejected during the `useSettings.ts` type-safety fix, then properly resolved via a narrow, justified cast instead).
- **Accessibility (a11y)** — semantic HTML, ARIA attributes, keyboard navigation, focus management. Also only ever caught incidentally (`IconButton`/`CardHeaderChevron` needing real `aria-label`s, `Tabs.tsx` needing genuine `role="tablist"` + arrow-key navigation, `Accordion.tsx` needing to be a real `<button>` instead of a `div` with manually-reimplemented keyboard handling) — a dedicated pass would likely find more.
- **Security** — this app handles real OAuth tokens and writes to a GM's actual Google Sheets, so this matters more than average for an app this size. Worth checking `dangerouslySetInnerHTML` usage, how tokens are stored/read, and injection risk from user-generated content (NPC names, notes, etc.) rendered without sanitization. One real OAuth CSRF bug was already found and fixed earlier this session — a dedicated security pass might find more in the same vein.
- **Performance** — unnecessary re-renders and missing memoization, checked carefully rather than blanket-applying `useMemo`/`useCallback` everywhere (this session already learned that lesson directly — the `useAppState.ts`/`useMoodPresets.ts` fixes were about correcting genuinely broken memoization, not adding memoization indiscriminately). Also worth checking for expensive computations re-running on every render unnecessarily.
- **Error handling consistency** — server-side error-response shaping was already audited (Category 6 above), but client-side async error handling hasn't been checked for consistency — are failures always caught, and always surfaced to the GM the same way, or does the pattern vary file to file?
- **Code smells beyond duplication** — magic numbers/strings that should be named constants, deeply nested conditionals, dead code or unused exports, "god functions" doing too many unrelated things (a function can be short in line count and still be doing 5 unrelated things at once — a different problem than file size, which Phase 2's census already covers).

Not yet scoped as concrete audit prompts — to be run the same way as the componentization audit (isolated, evidence-required categories) once prioritized.